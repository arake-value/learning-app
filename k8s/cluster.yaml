apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: learning-app-cluster
  region: ap-northeast-1
  version: "1.35"

managedNodeGroups:
  - name: app-ng
    instanceType: t3.medium
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
    volumeSize: 20